import OpportunityListPage from "../components/OpportunityListPage";

export default function WorkshopsPage() {
  return (
    <OpportunityListPage
      type="workshop"
      title="Workshops"
      description="Discover practical workshops and short learning sessions where students can gain skills, meet peers, and take part locally."
    />
  );
}
